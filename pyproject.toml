[project]
name = "camera-tracking"
version = "0.1.0"
description = "Camera-based people flow, density, and movement tracking for indoor rooms."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"
