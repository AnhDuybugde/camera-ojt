# Cai dat chuan (khuyen dung):
#   python -m pip install -e ".[face,store,reid,voice,dev]"
# File nay chi de `pip install -r requirements.txt` van chay duoc;
# no cai project o che do editable kem day du extra.
-e .[face,store,reid,voice,dev]

# Ghi chu:
# - Can them system package: ffmpeg (mic RTSP + TTS), portaudio/pulseaudio
#   tren Linux cho mic/loa ban local (pulseaudio-utils).
# - De dung CUDA: cai ban PyTorch tuong thich GPU/driver TRUOC, vi du:
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   roi kiem tra: python -c "import torch; print(torch.cuda.is_available())"
# - onnxruntime-gpu (keo theo qua insightface) can thu vien NVIDIA tu pip
#   (nvidia-cublas-cu12, nvidia-cudnn-cu12...); code tu nap qua LD_LIBRARY_PATH.
